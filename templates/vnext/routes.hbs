/* eslint-disable */
/* ------------------------------------------------------------
 File auto-generated

 @summary {{astDocument.title}}
 @description {{astDocument.description}}
 @version {{astDocument.version}}
------------------------------------------------------------ */

{{#astDocument}}

{{#if operations}}
import { handleRequestValidatorErrors, handleValidation, serializeResponse } from '@shared/utilities/openapi.utilities.js';
import { routerAuthMiddleware } from '@middleware/router.auth.middleware.js';
import * as models from './{{lowercase (replace title (toRegex "(-|\.| )+" "g") "_")}}.models.js';
import * as validators from './{{lowercase (replace title (toRegex "(-|\.| )+" "g") "_")}}.validations.js';
import * as handlers from '@modules/{{lowercase (replace apiName (toRegex "(-|\.| )+" "g") "-")}}/routes/{{lowercase (replace title (toRegex "(-|\.| )+" "g") "_")}}/{{lowercase (replace title (toRegex "(-|\.| )+" "g") "_")}}.handlers.js';
import { {{replace apiName (toRegex "(-|\.| )+" "g") "_"}}Router } from '@modules/{{lowercase (replace apiName (toRegex "(-|\.| )+" "g") "-")}}/routes/router.js'

{{>references}}

{{#each operations}}
{{>declaration prefix=(replace ../apiName (toRegex "(-|\.| )+" "g") "_")}}
{{/each}}

{{/if}}

{{/astDocument}}